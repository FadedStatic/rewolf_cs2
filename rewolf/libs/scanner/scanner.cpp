#include "scanner.hpp"

namespace scanner_utils
{
	std::optional<std::uintptr_t> pattern_scan(const std::string& mod_name, const std::string& aob, const std::string& mask)
	{
		return std::nullopt;
	}

}